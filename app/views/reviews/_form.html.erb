<%= render "layouts/errors", :object => @review %>

<%= form_for [@product, @review] do |f| %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.label :comment %>
  <%= f.text_field :comment %>
  <%= f.submit "Add Review", class: "btn black" %>
<% end %>
